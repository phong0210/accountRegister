server:
  port: 8080
  servlet:
    context-path: /account


spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/account"
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true